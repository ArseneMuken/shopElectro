{% extends 'bases.html.twig' %}

{% block body %}
<div class="content-wrapper">
    <div class="container-fluid">
        <h1>Marque creation</h1>
        <div class="card mb-3">
            <div class="card-header">
                <i class="fa fa-table"></i> Donnée</div>
            <div class="card-body">
                <div class="table-responsive">


    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-danger" value="Créer"  />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('marque_index') }}" class="color">Retour à la Liste</a>
        </li>
    </ul>
                    <div class="card-footer small text-muted"></div>
                </div></div></div></div>

</div>
{% endblock %}
