<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top "style="background-color: #c43235;">
        <a class="navbar-brand mb-0 h1" href="{{ path('listeProduit') }}">BiTech</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('listeProduit') }}">Accueil <span class="sr-only">(current)</span></a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Administration</a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{path('article_index') }}">Liste article</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{path('article_new') }}">Creation article</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{path('categorie_index') }}">Liste categorie</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{path('categorie_new') }}">Créer categorie</a>
                        </div>
                    </li>

                {#<li class="nav-item active"><a class="nav-link" href="{{path('admin') }}">menu_administrateur</a></li>#}
                {% endif %}
            </ul>
        </div>
        <div>
            <ul class="navbar-nav px-3">
                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_USER') %}
                    <li class="nav-item active">
                        <a class="nav-link " href="#"><i class="large material-icons">add_shopping_cart</i></a>
                    </li>
                    <li class="navbar-brand mb-0 h2 "><a href="{{path('fos_user_security_logout') }}">Se deconnecter</a></li>
                {% else %}
                    <li class="navbar-brand mb-0 h2"><a href="{{path('fos_user_security_login') }}">Se connecter</a></li>
                    <li class="navbar-brand mb-0 h2"><a href="{{path('fos_user_registration_register') }}">S'inscrire</a></li>
                {% endif %}

            </ul>
        </div>
    </nav>
</header>